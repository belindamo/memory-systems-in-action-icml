[project]
name = "lme-plus"
version = "0.1.0"
description = "LME+ Evaluation Framework"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
